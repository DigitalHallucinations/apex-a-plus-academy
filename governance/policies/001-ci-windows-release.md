---
id: 001
title: 'CI gate: Windows release'
status: active
applies_to: '.github/workflows/release.yml'
---

# 001: CI gate — Windows release

The workflow [`.github/workflows/release.yml`](.github/workflows/release.yml) is a binding gate adopted into the
pact. It must pass before merge; disabling or weakening it requires operator approval (INV-2).
